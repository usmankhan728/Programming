#include<iostream>
#include<cmath>
using namespace std;

int main(){
    float num;
    cout<<"enter the number : ";
    cin>>num;
    cout<<"the squre root of the "<<num<<" is "<<sqrt(num)<<endl;
    return 0;
}